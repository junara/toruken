<div class="panel panel-default">
    <div class="panel-heading">
        測定値一覧
    </div>
    <div class="panel-body">
        <div class="row">
        <% @photos.each do |photo| %>
          <%= render partial: 'photos/list', locals: { photo: photo } %>
        <% end %>
    </div>
    </div>
</div>
