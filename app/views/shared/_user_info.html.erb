<%= link_to gravatar_for(current_user, size: 50), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to "私の測定値を見る", current_user %></span>
<span><%= pluralize(current_user.photos.count, "測定") %></span>
